# Contributing

Install Node.js 20+, run `npm install`, then use `npm run dev`. Before opening a pull request, run `npm test`, `npm run lint`, `npm run typecheck`, and `npm run build`. Keep Plex and Letterboxd fixtures mocked in tests and never commit credentials, real user data, or real library data.
