import { NextResponse } from "next/server";
import { getLibraries, PlexError, resolveServer } from "@/lib/plex";
import { getSession } from "@/lib/request";
export async function GET(request: Request) { const session = await getSession(); const id = new URL(request.url).searchParams.get("server"); if (!session) return NextResponse.json({ error: "Connect Plex to continue." }, { status: 401 }); if (!id) return NextResponse.json({ error: "Choose a valid Plex server." }, { status: 400 }); try { const connection = await resolveServer(session.token, session.clientId, id); return NextResponse.json({ libraries: await getLibraries(connection, session.clientId) }); } catch (error) { const plexError = error as PlexError; return NextResponse.json({ error: plexError.message, diagnostics: plexError.diagnostics }, { status: 502 }); } }
